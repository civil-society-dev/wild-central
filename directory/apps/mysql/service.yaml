apiVersion: v1
kind: Service
metadata:
  name: mysql
  namespace: mysql
spec:
  type: ClusterIP
  ports:
    - name: mysql
      port: {{ .apps.mysql.port }}
      protocol: TCP
      targetPort: mysql
  selector:
    component: primary